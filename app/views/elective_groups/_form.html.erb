<%= form_for(@elective_group) do |f| %>
  <% if @elective_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@elective_group.errors.count, "error") %> prohibited this elective_group from being saved:</h2>

      <ul>
      <% @elective_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
