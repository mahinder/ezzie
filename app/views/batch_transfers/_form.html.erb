<%= form_for(@batch_transfer) do |f| %>
  <% if @batch_transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@batch_transfer.errors.count, "error") %> prohibited this batch_transfer from being saved:</h2>

      <ul>
      <% @batch_transfer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
